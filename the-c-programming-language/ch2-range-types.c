#include <stdio.h>
#include <limits.h>

int main() {

	int min, max;

	printf("int: %d to %d\n", INT_MIN, INT_MAX);
	printf("char: %d to %d\n", CHAR_MIN, CHAR_MAX);
	printf("long: %ld to %ld\n", LONG_MIN, LONG_MAX);
}